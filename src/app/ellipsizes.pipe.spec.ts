import { EllipsizesPipe } from './ellipsizes.pipe';

describe('EllipsizesPipe', () => {
  it('create an instance', () => {
    const pipe = new EllipsizesPipe();
    expect(pipe).toBeTruthy();
  });
});
